# =============================================================================
# claude-remote 环境变量模板
# 用法：cp .env.example .env，然后按需修改；.env 不要提交到 git
# =============================================================================

# ---------------------------------------------------------------------------
# Agent 安装（启动时按需安装，镜像里不预装 → 体积更小）
# AGENT 支持（逗号分隔组合）：
#   claude / codex / opencode / grok / cursor / kimi / copilot / agy / pi / none
#   opencode ：开源，自定义 OpenAI 兼容端点最灵活（国内网关友好），npm 安装
#   grok    ：xAI 官方 CLI，XAI_API_KEY env 即 headless 认证；x.ai 下载国内可能慢
#   cursor  ：Cursor Agent CLI，需 Cursor 账号，不支持自定义端点
#   kimi    ：Kimi Code CLI（已取代 kimi-cli），curl 安装，官方端点为主
#   copilot ：GitHub Copilot CLI，npm 安装，官方端点为主（GitHub 登录）
#   agy     ：Google Antigravity CLI，需 Google 账号，不支持自定义端点
#   pi      ：Pi coding agent，npm 安装，支持自定义 provider
# NPM_REGISTRY：npm 镜像地址。国内用户建议 https://registry.npmmirror.com
# ---------------------------------------------------------------------------
AGENT=claude
NPM_REGISTRY=https://registry.npmjs.org
# 版本锁定（可选，默认 latest；仅对 npm 安装的 agent 生效）：
# CLAUDE_VERSION=2.1.89
# CODEX_VERSION=0.5.0
# OPENCODE_VERSION=0.5.0
# COPILOT_VERSION=
# PI_VERSION=

# ---------------------------------------------------------------------------
# Claude Code API（方式一：环境变量）
# 使用第三方/代理 API 端点时，设置 ANTHROPIC_BASE_URL 指向你的网关地址
# （例如 OneAPI / NewAPI / DeepSeek 兼容端点等）
# ---------------------------------------------------------------------------
ANTHROPIC_BASE_URL=https://api.anthropic.com
ANTHROPIC_API_KEY=sk-ant-your-key-here

# 备选：用 Bearer token 代替 x-api-key（某些网关需要）
# ANTHROPIC_AUTH_TOKEN=

# ---------------------------------------------------------------------------
# Codex（OpenAI CLI，可选 —— 与 Claude Code 二选一或同时装）
# 用法：AGENT 里加 codex（如 AGENT=claude,codex），启动时会自动安装。
# 在 hapi Web UI 新建会话时可选择用哪个 agent。
# ---------------------------------------------------------------------------
# 方式 A：OpenAI 官方 API key（需一次性登录，登录缓存持久化在 codex-config 卷）：
#   docker exec -it claude-remote bash -c 'printenv OPENAI_API_KEY | codex login --with-api-key'
# OPENAI_API_KEY=sk-...

# 方式 B：第三方/代理网关（设置后启动时自动生成 ~/.codex/config.toml，无需登录）：
# 注意：Codex 的 wire_api 目前只支持 "responses"，网关需兼容 OpenAI Responses API
#   （很多只支持 /v1/chat/completions 的中转站不兼容，请确认网关能力）。
# CODEX_BASE_URL=https://your-gateway.com/v1
# CODEX_API_KEY=sk-...
# CODEX_MODEL=gpt-5.1

# 方式 C：挂载自定义 ~/.codex/config.toml（文件方式，见 README）

# ---------------------------------------------------------------------------
# OpenCode / Grok / Cursor 认证
# ---------------------------------------------------------------------------
# OpenCode：默认读标准 provider 环境变量（如 OPENAI_API_KEY）
# OpenCode 自定义网关：设置下面变量后自动生成 ~/.config/opencode/opencode.json
#   （apiKey 用 {env:OPENCODE_API_KEY} 插值，密钥不落盘；不覆盖已挂载的文件）
# OPENCODE_BASE_URL=https://your-gateway.com/v1
# OPENCODE_API_KEY=sk-...
# OPENCODE_MODEL=gpt-4o-mini
# OPENAI_API_KEY=sk-...

# Grok：官方 xAI 端点用 XAI_API_KEY 环境变量即完成认证（无需交互登录）
# XAI_API_KEY=xai-...
# Grok 第三方中转：设置下面变量后自动生成 ~/.grok/config.toml（env_key 方式，无需登录）
#   api_backend 默认 chat_completions（中转站最通用），也可设 responses / messages
# GROK_BASE_URL=https://your-gateway.com/v1
# GROK_API_KEY=sk-...
# GROK_MODEL=grok-4.5
# GROK_API_BACKEND=chat_completions

# Cursor：首次在容器内执行 docker exec -it claude-remote agent login（需 Cursor 账号）；
# 注意：Cursor 锁死官方后端，不支持自定义端点/中转，API key 必须是 Cursor Dashboard 签发

# ---------------------------------------------------------------------------
# Copilot / Kimi / Pi 认证
# ---------------------------------------------------------------------------
# Copilot：走 GitHub 登录（copilot login）即可，官方端点为主（无 BYOK/自定义端点支持）
#   docker exec -it claude-remote copilot login

# Kimi Code：官方端点用 KIMI_API_KEY（Moonshot 控制台签发）即可
# KIMI_API_KEY=sk-...

# Pi：纯第三方 provider（无官方托管后端）。设置下面变量后自动生成
#   ~/.pi/agent/models.json（apiKey 写 $PI_API_KEY 环境变量插值，密钥不落盘）
# PI_BASE_URL=https://your-gateway.com/v1
# PI_API_KEY=sk-...
# PI_MODEL=gpt-4o-mini
# PI_API=openai-completions    # 可选：openai-completions(默认) / openai-responses / anthropic-messages / google-generative-ai

# ---------------------------------------------------------------------------
# HAPI（手机/浏览器远程控制）
# ---------------------------------------------------------------------------
# 远程通道的智能默认（一般不用设置 HAPI_NO_RELAY）：
#   - Tailscale 连接成功 → 自动走 Tailscale 中继（推荐，流量不出你的 tailnet）
#   - Tailscale 未配置/连接失败 → 自动走官方公共中继 relay.hapi.run（保底可用）
# 想强制指定时：
#   HAPI_NO_RELAY=true  → 只走 Tailscale/私网（同时设了 TS_AUTHKEY 才能远程访问）
#   HAPI_NO_RELAY=false → 强制走官方公共中继（可与 Tailscale 并存）
# HAPI_NO_RELAY=

# hub 监听地址：设置了 TS_AUTHKEY 时默认自动改为 0.0.0.0（tailnet 直连），
# 否则默认 127.0.0.1（仅 relay 出站连接，更安全）。一般无需手动设置。
# HAPI_LISTEN_HOST=127.0.0.1
# HAPI_LISTEN_PORT=3006

# HAPI_RELAY_FORCE_TCP：false（默认）/ true —— 仅在用官方公共中继且 UDP 不通（公司网络等）时强制走 TCP
# HAPI_RELAY_FORCE_TCP=false

# 可选：推送通知（审批权限时手机收到通知）
# TELEGRAM_BOT_TOKEN=
# SERVERCHAN_SENDKEY=

# ---------------------------------------------------------------------------
# Tailscale（推荐的主远程通道）
# 在管理后台 https://login.tailscale.com/admin/settings/keys 生成 auth key
# 留空 = 不启用 Tailscale，退回 HAPI 官方公共中继
# 启用 = 手机/电脑装 Tailscale App 登录同一 tailnet，浏览器访问
#        http://<tailnet-IP>:3006 即可控制 Claude Code（数据不走第三方）
#        同时获得 Tailscale SSH 直连：ssh claude@claude-vps
# ---------------------------------------------------------------------------
TS_AUTHKEY=
TS_HOSTNAME=claude-vps
# TS_SSH：true（默认）启用 Tailscale SSH / false 关闭
TS_SSH=true
# 额外的 tailscale up 参数（空格分隔），例如：--accept-routes --advertise-exit-node
TS_EXTRA_ARGS=
